[project]
name = "personal-finance-claw"
version = "0.1.0"
description = "Demo project that shows Microsoft agent framework called harness for a finance agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
